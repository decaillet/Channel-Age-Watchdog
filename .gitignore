# Secrets — never commit an API key or env file
.env
.env.*
*.key
secrets.json

# Packaged builds
web-ext-artifacts/
*.xpi
*.zip

# Node (in case we add web-ext / tooling later)
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
