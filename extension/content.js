// Channel Age Watchdog — content script
// M1: prove the extension loads and runs on YouTube. Does nothing useful yet.

console.log("[Channel Age Watchdog] content script loaded on:", location.href);
